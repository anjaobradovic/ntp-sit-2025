export default function NextPage() {
  return (
    <div style={{ padding: 24 }}>
      <h2 style={{ margin: 0 }}>Next page</h2>
      <p style={{ opacity: 0.75 }}>Blank for now — we’ll build the menu/game next.</p>
    </div>
  );
}
